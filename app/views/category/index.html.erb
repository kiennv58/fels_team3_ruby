<% provide(:title, 'Category') %>
<h1>category</h1>

<div>
<% @categories.each do |cat| %>
<div class="box col-md-12">
	<div class="col-md-6">
		<div class="row">
			<div class="khung">
				<h2> <%= cat.name %> </h2>
				<p> descriptions descriptions descriptions descriptions
					descriptions descriptions descriptions descriptions
					descriptions descriptions descriptions descriptions
					descriptions descriptions descriptions descriptions
				</p>
				<%= form_for Lesson.new do |f| %>
					<%= f.hidden_field :category_id, value: cat.id %>
					<%= f.submit "Start", class: "btn btn-default" %>
				<% end %>
			</div>
		</div>
	</div>
	<div class="col-md-6">
		<div class="row">
			<p><img
			alt="category-logo"
			src="/assets/11.jpg" width="50px" height="50px"/>
			</p>
		</div>
	</div>
</div>
<% end %>
</div>